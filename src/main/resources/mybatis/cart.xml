<?xml version="1.0" encoding="UTF-8" ?> 
 
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.cart.CartMapper">
	<select id="list" parameterType="String" resultType="com.study.cart.CartListDTO">
		select a.cartno, a.pname, a.quantity, b.price, b.filename, b.contentsno
		from cart a inner join contents b
		on a.pname = b.pname
		where a.id = #{id}
	</select>
</mapper>